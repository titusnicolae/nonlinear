#include <stdio.h>
struct node
{ bool tip;
  char* str; //+ - / * ^ s c ai
  node* list;
};

nod* termen(char *p,node *n)
{ 
  
   
}

int main ()
{ char c[]="2+3*5";

  prelucrare(c,new node);
  return 0;
}
